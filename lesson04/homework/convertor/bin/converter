#!/usr/local/bin/ruby
require 'bundler/setup'
Bundler.require
require_rel '../main.rb'

options = {}
#options = OptionParser.new

options = {input: "http://lenta.ru/rss", output: 'json', sort: 'asc'}

main_program = Main.new(options)
main_program.run