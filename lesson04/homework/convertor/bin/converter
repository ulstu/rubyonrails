#!/usr/local/bin/ruby
require 'bundler/setup'
Bundler.require
require_rel '../main.rb'

options = {}
#options = OptionParser.new
#input = "http://lenta.ru/rss"
input = 'rss'

options = {input: input, output: 'json', sort: 'asc'}

main_program = Main.new(options)
main_program.run